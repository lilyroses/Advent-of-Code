# Solution 1 - Advent of Code 2015, Day 15

INPUT_FILE = "input.txt"
with open(INPUT_FILE, "r") as f:
    lines = [line.strip() for line in f.readlines()]


def main():
    total_tsps = 100
    allocs = []
    a, b, c = 0, 0, 0
    d = total_tsps - (a + b + c)


    ingredient_scores = {}

    for line in lines:
        items = line.split()
        ingredient = items[0][:-1]
        capacity = int(items[2][:-1])
        durability = int(items[4][:-1])
        flavor = int(items[6][:-1])
        texture = int(items[8][:-1])
        calories = int(items[10])
        ingredient_scores[ingredient] = [capacity, durability, flavor, texture, calories]

    5.


if __name__ == "__main__":
    main()
